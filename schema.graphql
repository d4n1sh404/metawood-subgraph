type Listing @entity {
  id: ID!
  creator: Bytes! # address
  tokenId: BigInt!
  price: BigInt!
  open: Boolean!
}

type Nft @entity {
  id: ID!
  url: String!
}

type User @entity {
  id: ID!
  address: Bytes!
  name: String
  image: String
  email: String
  data: String!
}
